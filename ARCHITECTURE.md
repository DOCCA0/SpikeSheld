# 🏗️ SpikeShield Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        SpikeShield System                         │
└─────────────────────────────────────────────────────────────────┘

┌───────────────┐         ┌──────────────┐         ┌──────────────┐
│   Frontend    │         │   Backend    │         │  Blockchain  │
│   (React)     │◄────────┤     (Go)     │◄────────┤  (Ethereum)  │
└───────────────┘         └──────────────┘         └──────────────┘
       │                         │                         │
       │                         │                         │
       ▼                         ▼                         ▼
┌───────────────┐         ┌──────────────┐         ┌──────────────┐
│   MetaMask    │         │  PostgreSQL  │         │   Chainlink  │
│    Wallet     │         │   Database   │         │    Oracle    │
└───────────────┘         └──────────────┘         └──────────────┘
```

## Component Interaction Flow

### 1. Insurance Purchase Flow

```
User                Frontend              Blockchain           Backend
 │                     │                      │                  │
 │──Purchase──────────>│                      │                  │
 │   Insurance         │                      │                  │
 │                     │                      │                  │
 │                     │──Connect Wallet─────>│                  │
 │                     │      (MetaMask)      │                  │
 │                     │                      │                  │
 │<──Request Approval──│                      │                  │
 │   (USDT)            │                      │                  │
 │                     │                      │                  │
 │──Approve────────────>│──approve(USDT)─────>│                  │
 │                     │                      │                  │
 │                     │                      │                  │
 │<──Request Purchase──│                      │                  │
 │                     │                      │                  │
 │──Confirm────────────>│──buyInsurance()────>│                  │
 │                     │                      │                  │
 │                     │                      │──Emit Event──────>│
 │                     │                      │  PolicyPurchased │
 │                     │                      │                  │
 │                     │<──Transaction Hash───│                  │
 │<──Success Message───│                      │                  │
 │                     │                      │                  │
```

### 2. Spike Detection & Payout Flow

```
Data Source         Backend              Database          Blockchain
    │                  │                     │                 │
    │──Price Data─────>│                     │                 │
    │  (CSV/Oracle)    │                     │                 │
    │                  │                     │                 │
    │                  │──Store Prices──────>│                 │
    │                  │                     │                 │
    │                  │                     │                 │
    │                  │<──Historical Data───│                 │
    │                  │                     │                 │
    │                  │                     │                 │
    │                  │─[Detect Spike]      │                 │
    │                  │   Algorithm         │                 │
    │                  │   Running           │                 │
    │                  │                     │                 │
    │                  │──Store Spike───────>│                 │
    │                  │   Record            │                 │
    │                  │                     │                 │
    │                  │<──Active Policies───│                 │
    │                  │                     │                 │
    │                  │                     │                 │
    │                  │──executePayout()──────────────────────>│
    │                  │   (for each policy) │                 │
    │                  │                     │                 │
    │                  │                     │                 │
    │                  │<──Transaction Hash──┼─────────────────│
    │                  │                     │                 │
    │                  │──Store Payout──────>│                 │
    │                  │   Record            │                 │
    │                  │                     │                 │
```

## Module Architecture

### Frontend Module

```
┌─────────────────────────────────────────┐
│           Frontend (React)              │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────┐    ┌─────────────┐   │
│  │   App.js    │    │  useContract│   │
│  │  (Main UI)  │◄───┤    Hook     │   │
│  └─────────────┘    └─────────────┘   │
│                            │           │
│                            ▼           │
│  ┌─────────────┐    ┌─────────────┐   │
│  │ Components  │    │  ethers.js  │   │
│  │   (UI)      │    │  (Web3 lib) │   │
│  └─────────────┘    └─────────────┘   │
│                            │           │
└────────────────────────────┼───────────┘
                             │
                             ▼
                      ┌─────────────┐
                      │  MetaMask   │
                      │   Wallet    │
                      └─────────────┘
```

### Backend Module

```
┌─────────────────────────────────────────────────┐
│              Backend (Go)                       │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────┐                                  │
│  │ main.go  │  (Entry point & orchestration)   │
│  └────┬─────┘                                  │
│       │                                         │
│  ┌────▼──────────────────────────────────┐    │
│  │         Config & Utils                │    │
│  │  (Load config, logging, helpers)      │    │
│  └────┬──────────────────────────────────┘    │
│       │                                         │
│  ┌────▼────────┐  ┌────────────┐  ┌─────────┐│
│  │  DataFeed   │  │  Detector  │  │   API   ││
│  │  Module     │─>│   Module   │─>│  Module ││
│  └─────────────┘  └────────────┘  └─────────┘│
│       │                  │              │     │
│       ▼                  ▼              ▼     │
│  ┌─────────────────────────────────────────┐ │
│  │          Database (PostgreSQL)          │ │
│  │  - Prices   - Spikes                    │ │
│  │  - Policies - Payouts                   │ │
│  └─────────────────────────────────────────┘ │
│                                                │
└────────────────────────────────────────────────┘
```

### Smart Contract Module

```
┌─────────────────────────────────────────┐
│      Blockchain (Solidity)              │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │     InsurancePool.sol           │   │
│  ├─────────────────────────────────┤   │
│  │                                 │   │
│  │  State Variables:               │   │
│  │  - usdt (IERC20)                │   │
│  │  - premiumAmount                │   │
│  │  - coverageAmount               │   │
│  │  - userPolicies (mapping)       │   │
│  │                                 │   │
│  │  Functions:                     │   │
│  │  - buyInsurance()               │   │
│  │  - executePayout()              │   │
│  │  - getUserPolicies()            │   │
│  │  - hasActivePolicy()            │   │
│  │                                 │   │
│  └────────────┬────────────────────┘   │
│               │                         │
│  ┌────────────▼────────────────────┐   │
│  │       MockUSDT.sol              │   │
│  ├─────────────────────────────────┤   │
│  │                                 │   │
│  │  ERC20 Standard:                │   │
│  │  - balanceOf()                  │   │
│  │  - transfer()                   │   │
│  │  - approve()                    │   │
│  │  - transferFrom()               │   │
│  │                                 │   │
│  │  Custom:                        │   │
│  │  - mint() (for testing)         │   │
│  │                                 │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

## Data Flow Diagram

### Replay Mode

```
┌──────────────┐
│ CSV File     │
│ (Historical) │
└──────┬───────┘
       │
       │ 1. Load price data
       ▼
┌──────────────────┐
│ ReplayFeed       │
│ (datafeed pkg)   │
└──────┬───────────┘
       │
       │ 2. Parse & insert
       ▼
┌──────────────────┐
│ PostgreSQL       │
│ (prices table)   │
└──────┬───────────┘
       │
       │ 3. Query historical data
       ▼
┌──────────────────┐
│ Detector         │
│ (detect.go)      │
└──────┬───────────┘
       │
       │ 4. Spike found?
       ▼
┌──────────────────┐
│ PayoutService    │
│ (payout.go)      │
└──────┬───────────┘
       │
       │ 5. Execute payout tx
       ▼
┌──────────────────┐
│ Smart Contract   │
│ (on blockchain)  │
└──────────────────┘
```

### Live Mode

```
┌──────────────┐
│ Chainlink    │
│ Oracle       │
└──────┬───────┘
       │
       │ 1. Fetch latest price
       ▼
┌──────────────────┐
│ LiveFeed         │
│ (datafeed pkg)   │
└──────┬───────────┘
       │
       │ 2. Store price
       ▼
┌──────────────────┐
│ PostgreSQL       │
│ (prices table)   │
└──────┬───────────┘
       │
       │ 3. Continuous monitoring
       ▼
┌──────────────────┐
│ Detector         │
│ (running loop)   │
└──────┬───────────┘
       │
       │ 4. Spike detected?
       ▼
┌──────────────────┐
│ PayoutService    │
│ (payout.go)      │
└──────┬───────────┘
       │
       │ 5. Trigger payout
       ▼
┌──────────────────┐
│ Smart Contract   │
│ (on blockchain)  │
└──────────────────┘
```

## Database Schema

```
┌─────────────────┐
│     prices      │
├─────────────────┤
│ id (PK)         │
│ timestamp       │
│ symbol          │
│ open            │
│ high            │
│ low             │
│ close           │
│ volume          │
│ created_at      │
└────────┬────────┘
         │
         │ Referenced by
         │
┌────────▼────────┐
│     spikes      │
├─────────────────┤
│ id (PK)         │
│ timestamp       │
│ symbol          │
│ price_before    │
│ price_after     │
│ drop_percent    │
│ detected_at     │
└────────┬────────┘
         │
         │ Referenced by
         │
┌────────▼────────┐       ┌─────────────────┐
│    policies     │       │     payouts     │
├─────────────────┤       ├─────────────────┤
│ id (PK)         │◄──────│ id (PK)         │
│ user_address    │       │ policy_id (FK)  │
│ premium         │       │ user_address    │
│ coverage_amount │       │ amount          │
│ purchase_time   │       │ spike_id (FK)   │
│ expiry_time     │       │ tx_hash         │
│ status          │       │ executed_at     │
│ tx_hash         │       └─────────────────┘
│ created_at      │
└─────────────────┘
```

## Technology Stack

```
┌─────────────────────────────────────────────────┐
│                   Frontend                      │
│  React 18 + ethers.js 6 + MetaMask             │
└─────────────────────────────────────────────────┘
                      │
                      │ JSON-RPC
                      ▼
┌─────────────────────────────────────────────────┐
│                  Blockchain                     │
│  Solidity + OpenZeppelin + Hardhat             │
│  (Sepolia / BSC Testnet)                       │
└─────────────────────────────────────────────────┘
                      ▲
                      │ Contract Calls
                      │
┌─────────────────────┴───────────────────────────┐
│                   Backend                       │
│  Go 1.21 + go-ethereum + PostgreSQL driver     │
└─────────────────────────────────────────────────┘
                      │
                      │ SQL
                      ▼
┌─────────────────────────────────────────────────┐
│                  Database                       │
│  PostgreSQL 15                                  │
└─────────────────────────────────────────────────┘
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────┐
│              Docker Compose                      │
├──────────────────────────────────────────────────┤
│                                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────┐│
│  │  Frontend   │  │   Backend   │  │PostgreSQL││
│  │  Container  │  │  Container  │  │Container ││
│  │  (Node:18)  │  │  (Go:1.21)  │  │(PG:15)   ││
│  │             │  │             │  │          ││
│  │ Port: 3000  │  │  CLI only   │  │Port: 5432││
│  └─────────────┘  └─────────────┘  └──────────┘│
│                                                  │
└──────────────────────────────────────────────────┘
           │                   │
           ▼                   ▼
    ┌────────────┐      ┌──────────────┐
    │   User's   │      │   Testnet    │
    │  Browser   │      │  Blockchain  │
    └────────────┘      └──────────────┘
```

## Security Architecture

```
┌────────────────────────────────────────┐
│         Security Layers                │
├────────────────────────────────────────┤
│                                        │
│  Frontend:                             │
│  - Input validation                    │
│  - Transaction preview                 │
│  - MetaMask signature required         │
│                                        │
│  Smart Contract:                       │
│  - ReentrancyGuard                     │
│  - Ownable (access control)            │
│  - Input validation                    │
│  - Event logging                       │
│                                        │
│  Backend:                              │
│  - Oracle role verification            │
│  - Database parameterized queries      │
│  - Private key secure storage          │
│  - Error handling                      │
│                                        │
│  Database:                             │
│  - Connection pooling                  │
│  - SQL injection prevention            │
│  - Constraints & indexes               │
│                                        │
└────────────────────────────────────────┘
```

---

This architecture supports:
- ✅ Dual operational modes (Replay/Live)
- ✅ Automated spike detection
- ✅ Autonomous payout execution
- ✅ Scalable design
- ✅ Clear separation of concerns
- ✅ Easy deployment with Docker
